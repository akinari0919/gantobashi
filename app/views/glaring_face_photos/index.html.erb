<main>
  <div class = 'top-contents'>
    <% if @glaring_face_photos.present? %>
      <% if current_user.glaring_face_photos.count < 3 %>
        <%= link_to 'New', new_glaring_face_photo_path %>
      <% end %>
      <br>
      <%= link_to 'HideAll', glaring_face_photos_hide_path %>
      <br>
      <% @glaring_face_photos.each do |gfp| %>
        <img src="data:image/jpeg;base64,<%="#{gfp.image}"%>" width="100" height="100" />
        <%= gfp.face_score %>
        <%= gfp.battle_win_count %>
        <%= link_to 'Delete', glaring_face_photo_path(gfp), method: :delete %>
        <%= form_with model: gfp, local: true do |f| %>
          <%= f.submit %>
        <% end %>
        <br>
      <% end %>
    <% else %>
      <%= link_to 'New', new_glaring_face_photo_path %>
    <% end %>
      <%= link_to 'Back', profile_path %>
  </div>
</main>
